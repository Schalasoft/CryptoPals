namespace CryptoPals
{
    // The class just serves as a place to hold global constants throughout the program
    /// <summary>
    /// This class simply stores data that is used across all challenges
    /// </summary>
    static class Constants
    {
        // Separator this solution uses to seperate data (lines in a string, input/output etc.)
        public const string Separator = "/";
    }
}
